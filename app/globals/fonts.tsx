import { Poppins, Doto } from "next/font/google";

export const poppins = Poppins({subsets: ['latin'], weight: "400"})
export const dotoReg = Doto({subsets: ['latin'], weight: "400"})
export const dotoBlack = Doto({subsets: ['latin'], weight: "900"})